# Validate attributes.csv

Validate attributes.csv

## Usage

``` r
validate_attributes(attributes)
```

## Arguments

- attributes:

  (data.frame) A `data.frame` read in from `attributes.csv`

## Value

Nothing. Side-effect: Can `stop` execution if validation fails.
