# Validate creators.csv

Validate creators.csv

## Usage

``` r
validate_creators(creators)
```

## Arguments

- creators:

  (data.frame) A `data.frame` read in from `creators.csv`

## Value

Nothing. Side-effect: Can `stop` execution if validation fails.
