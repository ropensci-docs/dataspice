# Crosswalk a term

Crosswalk a term

## Usage

``` r
crosswalk(doc, term)
```

## Arguments

- doc:

  (list) A `dataspice` document as a `list`

- term:

  (character) The term to crosswalk.

## Value

(list) The result of the crosswalk. May be an empty `list` on failure.
