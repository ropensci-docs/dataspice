# Crosswalk functions for `as_eml` Crosswalk a Schema.org/Person

Crosswalk functions for `as_eml` Crosswalk a Schema.org/Person

## Usage

``` r
crosswalk_Person(creator)
```

## Arguments

- creator:

  (list) A creator
