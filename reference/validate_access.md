# Validate access.csv

Validate access.csv

## Usage

``` r
validate_access(access)
```

## Arguments

- access:

  (data.frame) A `data.frame` read in from `access.csv`

## Value

Nothing. Side-effect: Can `stop` execution if validation fails.
